services:
  - type: web
    name: twigg-v2-back
    env: python
    rootDir: twigg-v2-back
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
    plan: free
